# Optional. If set, Query Data and Query Features work immediately for
# every visitor without anyone connecting a provider on Module 6 first.
# The AI Use Case Library (Module 6) always prefers whatever the signed-in
# user has connected there — this key is only the fallback.
ANTHROPIC_API_KEY=

# Optional. Defaults to 8787.
PORT=8787
