{
  "name": "journi-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "journi backend — Express API + SQLite persistence; also serves the built frontend.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  }
}
