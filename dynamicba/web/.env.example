# Optional. Leave unset for local development — Vite proxies /api to the server
# (see vite.config.js). Set this only when the built web app is deployed separately
# from the API server, pointing at that server's full base URL, e.g.:
# VITE_API_URL=https://api.example.com/api
